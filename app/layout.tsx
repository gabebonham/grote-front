import type { Metadata } from 'next'

import './globals.css'
import localFont from 'next/font/local'
const dosis = localFont({
  src: '../public/fonts/static/Dosis-SemiBold.ttf',
  display: 'swap',
  weight: '900',
})

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${dosis.className}`}>
        {children}
        <script data-liveedit-widget data-project="531af981-4528-4eb1-9123-350eca132edb" dangerouslySetInnerHTML={{ __html: `
(function() {
  var projectId = '531af981-4528-4eb1-9123-350eca132edb';
  var conversation = [];

  var host = document.createElement('div');
  host.id = 'chat-widget-host-' + projectId;
  host.style.cssText = 'position:fixed;bottom:24px;right:24px;z-index:999999;display:flex;flex-direction:column;align-items:flex-end;';
  document.body.appendChild(host);

  var shadow = host.attachShadow({ mode: 'open' });

  var style = document.createElement('style');
  style.textContent = [
    '*{box-sizing:border-box;margin:0;padding:0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;}',
    '.bubble{width:56px;height:56px;border-radius:50%;background:#1a1a1a;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 4px 16px rgba(0,0,0,0.25);transition:transform 0.2s,box-shadow 0.2s;flex-shrink:0;}',
    '.bubble:hover{transform:scale(1.08);box-shadow:0 6px 20px rgba(0,0,0,0.32);}',
    '.bubble svg{width:26px;height:26px;fill:none;stroke:#fff;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}',
    '.panel{width:360px;height:520px;background:#fff;border-radius:16px;box-shadow:0 8px 40px rgba(0,0,0,0.18);display:flex;flex-direction:column;overflow:hidden;margin-bottom:12px;transform-origin:bottom right;transition:transform 0.28s cubic-bezier(0.34,1.56,0.64,1),opacity 0.22s ease;}',
    '.panel.hidden{transform:scale(0.7) translateY(40px);opacity:0;pointer-events:none;}',
    '.panel.visible{transform:scale(1) translateY(0);opacity:1;pointer-events:all;}',
    '.header{background:#1a1a1a;padding:16px 18px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0;}',
    '.header-left{display:flex;align-items:center;gap:10px;}',
    '.avatar{width:34px;height:34px;border-radius:50%;background:#3a3a3a;display:flex;align-items:center;justify-content:center;}',
    '.avatar svg{width:18px;height:18px;fill:none;stroke:#fff;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}',
    '.header-title{color:#fff;font-size:15px;font-weight:600;line-height:1.2;}',
    '.header-sub{color:#aaa;font-size:11px;margin-top:1px;}',
    '.close-btn{background:none;border:none;cursor:pointer;padding:4px;border-radius:6px;display:flex;align-items:center;justify-content:center;transition:background 0.15s;}',
    '.close-btn:hover{background:rgba(255,255,255,0.12);}',
    '.close-btn svg{width:18px;height:18px;stroke:#fff;stroke-width:2.2;stroke-linecap:round;}',
    '.messages{flex:1;overflow-y:auto;padding:16px 14px;display:flex;flex-direction:column;gap:10px;scroll-behavior:smooth;}',
    '.messages::-webkit-scrollbar{width:4px;}',
    '.messages::-webkit-scrollbar-track{background:transparent;}',
    '.messages::-webkit-scrollbar-thumb{background:#ddd;border-radius:4px;}',
    '.msg-row{display:flex;align-items:flex-end;gap:7px;}',
    '.msg-row.user{flex-direction:row-reverse;}',
    '.msg-avatar{width:26px;height:26px;border-radius:50%;background:#e8e8e8;display:flex;align-items:center;justify-content:center;flex-shrink:0;}',
    '.msg-avatar svg{width:14px;height:14px;stroke:#666;stroke-width:2;fill:none;stroke-linecap:round;stroke-linejoin:round;}',
    '.msg-bubble{max-width:76%;padding:10px 13px;border-radius:16px;font-size:14px;line-height:1.5;word-break:break-word;}',
    '.msg-bubble.user{background:#1a1a1a;color:#fff;border-bottom-right-radius:4px;}',
    '.msg-bubble.assistant{background:#f1f1f1;color:#1a1a1a;border-bottom-left-radius:4px;}',
    '.msg-bubble.error{background:#fff0f0;color:#c0392b;border:1px solid #ffd5d5;}',
    '.typing{display:flex;align-items:center;gap:5px;padding:12px 14px;}',
    '.dot{width:7px;height:7px;border-radius:50%;background:#bbb;animation:bounce 1.2s infinite ease-in-out;}',
    '.dot:nth-child(1){animation-delay:0s;}',
    '.dot:nth-child(2){animation-delay:0.2s;}',
    '.dot:nth-child(3){animation-delay:0.4s;}',
    '@keyframes bounce{0%,60%,100%{transform:translateY(0);}30%{transform:translateY(-7px);}}',
    '.input-row{padding:12px 14px;border-top:1px solid #f0f0f0;display:flex;gap:8px;align-items:flex-end;flex-shrink:0;background:#fff;}',
    '.input-field{flex:1;border:1.5px solid #e5e5e5;border-radius:22px;padding:10px 15px;font-size:14px;outline:none;resize:none;min-height:42px;max-height:100px;line-height:1.45;transition:border-color 0.18s;overflow-y:auto;color:#1a1a1a;}',
    '.input-field::placeholder{color:#bbb;}',
    '.input-field:focus{border-color:#1a1a1a;}',
    '.send-btn{width:42px;height:42px;border-radius:50%;background:#1a1a1a;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:background 0.18s,transform 0.15s;}',
    '.send-btn:hover{background:#333;transform:scale(1.06);}',
    '.send-btn:disabled{background:#ccc;cursor:default;transform:none;}',
    '.send-btn svg{width:17px;height:17px;fill:none;stroke:#fff;stroke-width:2.2;stroke-linecap:round;stroke-linejoin:round;margin-left:2px;}',
  ].join('');
  shadow.appendChild(style);

  var panel = document.createElement('div');
  panel.className = 'panel hidden';
  panel.innerHTML = [
    '<div class="header">',
      '<div class="header-left">',
        '<div class="avatar">',
          '<svg viewBox="0 0 24 24"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>',
        '</div>',
        '<div>',
          '<div class="header-title">Chat with us</div>',
          '<div class="header-sub">We typically reply instantly</div>',
        '</div>',
      '</div>',
      '<button class="close-btn" aria-label="Close chat">',
        '<svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>',
      '</button>',
    '</div>',
    '<div class="messages"></div>',
    '<div class="input-row">',
      '<textarea class="input-field" placeholder="Type a message..." rows="1"></textarea>',
      '<button class="send-btn" aria-label="Send message">',
        '<svg viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>',
      '</button>',
    '</div>',
  ].join('');
  shadow.appendChild(panel);

  var bubble = document.createElement('button');
  bubble.className = 'bubble';
  bubble.setAttribute('aria-label', 'Open chat');
  bubble.innerHTML = '<svg viewBox="0 0 24 24"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>';
  shadow.appendChild(bubble);

  var messagesEl = panel.querySelector('.messages');
  var inputEl = panel.querySelector('.input-field');
  var sendBtn = panel.querySelector('.send-btn');
  var closeBtn = panel.querySelector('.close-btn');

  var isOpen = false;
  var isLoading = false;

  function openPanel() {
    isOpen = true;
    panel.classList.remove('hidden');
    panel.classList.add('visible');
    bubble.setAttribute('aria-label', 'Close chat');
    setTimeout(function() { inputEl.focus(); }, 100);
    if (conversation.length === 0) {
      addMessage('assistant', 'Hi there! 👋 How can I help you today?');
    }
  }

  function closePanel() {
    isOpen = false;
    panel.classList.remove('visible');
    panel.classList.add('hidden');
    bubble.setAttribute('aria-label', 'Open chat');
  }

  bubble.addEventListener('click', function() {
    if (isOpen) { closePanel(); } else { openPanel(); }
  });

  closeBtn.addEventListener('click', function() {
    closePanel();
  });

  function scrollToBottom() {
    messagesEl.scrollTop = messagesEl.scrollHeight;
  }

  function createMsgAvatarSVG(isUser) {
    if (isUser) {
      return '<svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>';
    }
    return '<svg viewBox="0 0 24 24"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>';
  }

  function addMessage(role, text) {
    var isUser = role === 'user';
    var isError = role === 'error';

    var row = document.createElement('div');
    row.className = 'msg-row' + (isUser ? ' user' : '');

    var avatarDiv = document.createElement('div');
    avatarDiv.className = 'msg-avatar';
    avatarDiv.innerHTML = createMsgAvatarSVG(isUser);

    var msgBubble = document.createElement('div');
    msgBubble.className = 'msg-bubble ' + (isError ? 'error' : (isUser ? 'user' : 'assistant'));
    msgBubble.textContent = text;

    if (isUser) {
      row.appendChild(msgBubble);
      row.appendChild(avatarDiv);
    } else {
      row.appendChild(avatarDiv);
      row.appendChild(msgBubble);
    }

    messagesEl.appendChild(row);
    scrollToBottom();

    if (!isError) {
      conversation.push({ role: isUser ? 'user' : 'assistant', content: text });
    }

    return msgBubble;
  }

  function showTyping() {
    var row = document.createElement('div');
    row.className = 'msg-row';
    row.id = 'typing-indicator';

    var avatarDiv = document.createElement('div');
    avatarDiv.className = 'msg-avatar';
    avatarDiv.innerHTML = createMsgAvatarSVG(false);

    var typingBubble = document.createElement('div');
    typingBubble.className = 'msg-bubble assistant typing';
    typingBubble.style.padding = '10px 14px';
    typingBubble.innerHTML = '<div class="dot"></div><div class="dot"></div><div class="dot"></div>';

    row.appendChild(avatarDiv);
    row.appendChild(typingBubble);
    messagesEl.appendChild(row);
    scrollToBottom();
    return row;
  }

  function removeTyping(el) {
    if (el && el.parentNode) {
      el.parentNode.removeChild(el);
    }
  }

  function simulateAPI(userMessage, callback) {
    var delay = 800 + Math.random() * 600;
    setTimeout(function() {
      try {
        var reply = 'Hello! I received: ' + userMessage;
        callback(null, reply);
      } catch(e) {
        callback(new Error('Unexpected error'));
      }
    }, delay);
  }

  function sendMessage() {
    if (isLoading) return;
    var text = inputEl.value.trim();
    if (!text) return;

    inputEl.value = '';
    inputEl.style.height = 'auto';
    sendBtn.disabled = true;
    isLoading = true;

    addMessage('user', text);

    var typingRow = showTyping();

    simulateAPI(text, function(err, reply) {
      removeTyping(typingRow);
      isLoading = false;
      sendBtn.disabled = false;

      if (err) {
        addMessage('error', '⚠️ Something went wrong. Please try again.');
      } else {
        addMessage('assistant', reply);
      }

      inputEl.focus();
    });
  }

  sendBtn.addEventListener('click', function() {
    sendMessage();
  });

  inputEl.addEventListener('keydown', function(e) {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      sendMessage();
    }
  });

  inputEl.addEventListener('input', function() {
    this.style.height = 'auto';
    this.style.height = Math.min(this.scrollHeight, 100) + 'px';
  });
})();
` }} />
      </body>
    </html>
  )
}
