import type { Metadata } from 'next'

import './globals.css'
import localFont from 'next/font/local'
const dosis = localFont({
  src: '../public/fonts/static/Dosis-SemiBold.ttf',
  display: 'swap',
  weight: '900',
})

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${dosis.className}`}>
        {children}
        
        <script data-liveedit-widget data-project="a748b3eb-c989-43c1-b92d-f25a1920e0a3" dangerouslySetInnerHTML={{ __html: `
const PROJECT_ID = "a748b3eb-c989-43c1-b92d-f25a1920e0a3";
const API_ENDPOINT = "https://ai-editor-backend.vercel.app/api/chat";

(function () {
  const PROJECT_ID = "a748b3eb-c989-43c1-b92d-f25a1920e0a3";
  const API_ENDPOINT = "https://ai-editor-backend.vercel.app/api/chat";

  let conversation = [];
  let pendingChange = null;
  let pendingChangeMessageEl = null;

  // ── Host element + Shadow DOM ──────────────────────────────────────────────
  const host = document.createElement("div");
  host.id = "__chat-widget-host__";
  document.body.appendChild(host);

  const shadow = host.attachShadow({ mode: "open" });

  // ── Styles ─────────────────────────────────────────────────────────────────
  const style = document.createElement("style");
  style.textContent = \`
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    #launcher {
      position: fixed;
      bottom: 24px;
      right: 24px;
      z-index: 999999;
      width: 56px;
      height: 56px;
      border-radius: 50%;
      background: #1a1a1a;
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 16px rgba(0,0,0,0.3);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    #launcher:hover {
      transform: scale(1.08);
      box-shadow: 0 6px 20px rgba(0,0,0,0.38);
    }
    #launcher svg { pointer-events: none; }

    #panel {
      position: fixed;
      bottom: 92px;
      right: 24px;
      z-index: 999999;
      width: 360px;
      height: 520px;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 8px 40px rgba(0,0,0,0.18);
      display: flex;
      flex-direction: column;
      overflow: hidden;
      transform-origin: bottom right;
      transform: scale(0.85) translateY(16px);
      opacity: 0;
      pointer-events: none;
      transition: transform 0.25s cubic-bezier(0.34,1.56,0.64,1), opacity 0.2s ease;
    }
    #panel.open {
      transform: scale(1) translateY(0);
      opacity: 1;
      pointer-events: all;
    }

    /* Header */
    #header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 16px 18px;
      background: #1a1a1a;
      color: #fff;
      flex-shrink: 0;
    }
    #header-title {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      font-size: 15px;
      font-weight: 600;
      letter-spacing: 0.01em;
    }
    #close-btn {
      background: none;
      border: none;
      cursor: pointer;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 4px;
      border-radius: 6px;
      transition: background 0.15s;
    }
    #close-btn:hover { background: rgba(255,255,255,0.15); }

    /* Messages */
    #messages {
      flex: 1;
      overflow-y: auto;
      padding: 16px 14px;
      display: flex;
      flex-direction: column;
      gap: 10px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      font-size: 14px;
    }
    #messages::-webkit-scrollbar { width: 4px; }
    #messages::-webkit-scrollbar-thumb { background: #ddd; border-radius: 4px; }

    .msg-row {
      display: flex;
      flex-direction: column;
    }
    .msg-row.user { align-items: flex-end; }
    .msg-row.assistant { align-items: flex-start; }

    .bubble {
      max-width: 82%;
      padding: 9px 13px;
      border-radius: 14px;
      line-height: 1.5;
      word-break: break-word;
    }
    .msg-row.user .bubble {
      background: #1a1a1a;
      color: #fff;
      border-bottom-right-radius: 4px;
    }
    .msg-row.assistant .bubble {
      background: #f0f0f0;
      color: #1a1a1a;
      border-bottom-left-radius: 4px;
    }

    /* Typing dots */
    .typing-dots {
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 10px 14px;
    }
    .typing-dots span {
      width: 7px;
      height: 7px;
      background: #999;
      border-radius: 50%;
      display: inline-block;
      animation: bounce 1.2s infinite ease-in-out;
    }
    .typing-dots span:nth-child(2) { animation-delay: 0.2s; }
    .typing-dots span:nth-child(3) { animation-delay: 0.4s; }
    @keyframes bounce {
      0%, 80%, 100% { transform: translateY(0); opacity: 0.5; }
      40% { transform: translateY(-6px); opacity: 1; }
    }

    /* Confirmation buttons */
    .confirm-actions {
      display: flex;
      gap: 8px;
      margin-top: 7px;
    }
    .btn-confirm, .btn-undo {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      font-size: 13px;
      font-weight: 500;
      padding: 6px 13px;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      transition: opacity 0.15s, transform 0.1s;
    }
    .btn-confirm:hover, .btn-undo:hover { opacity: 0.82; transform: scale(1.03); }
    .btn-confirm {
      background: #1a1a1a;
      color: #fff;
    }
    .btn-undo {
      background: #efefef;
      color: #555;
    }

    /* Input row */
    #input-row {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 12px 14px;
      border-top: 1px solid #ebebeb;
      flex-shrink: 0;
      background: #fff;
    }
    #msg-input {
      flex: 1;
      border: 1px solid #e0e0e0;
      border-radius: 10px;
      padding: 9px 12px;
      font-size: 14px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      outline: none;
      color: #1a1a1a;
      background: #fafafa;
      transition: border-color 0.15s;
      resize: none;
    }
    #msg-input:focus { border-color: #1a1a1a; background: #fff; }
    #send-btn {
      width: 38px;
      height: 38px;
      flex-shrink: 0;
      background: #1a1a1a;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: opacity 0.15s, transform 0.1s;
    }
    #send-btn:hover { opacity: 0.82; transform: scale(1.05); }
    #send-btn:disabled { opacity: 0.4; cursor: default; transform: none; }
  \`;

  // ── Markup ─────────────────────────────────────────────────────────────────
  const launcher = document.createElement("button");
  launcher.id = "launcher";
  launcher.setAttribute("aria-label", "Open chat");
  launcher.innerHTML = \`
    <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
    </svg>\`;

  const panel = document.createElement("div");
  panel.id = "panel";
  panel.setAttribute("role", "dialog");
  panel.setAttribute("aria-label", "Chat");
  panel.innerHTML = \`
    <div id="header">
      <span id="header-title">Chat with us</span>
      <button id="close-btn" aria-label="Close chat">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <div id="messages"></div>
    <div id="input-row">
      <input id="msg-input" type="text" placeholder="Type a message\u2026" autocomplete="off" />
      <button id="send-btn" aria-label="Send">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/>
        </svg>
      </button>
    </div>\`;

  shadow.appendChild(style);
  shadow.appendChild(launcher);
  shadow.appendChild(panel);

  // ── Refs ───────────────────────────────────────────────────────────────────
  const messagesEl = shadow.getElementById("messages");
  const inputEl    = shadow.getElementById("msg-input");
  const sendBtn    = shadow.getElementById("send-btn");
  const closeBtn   = shadow.getElementById("close-btn");

  // ── Helpers ────────────────────────────────────────────────────────────────
  function formatMarkdown(text) {
    // Escape HTML first
    let s = text
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;");
    // **bold**
    s = s.replace(/\\*\\*(.+?)\\*\\*/g, "<strong>$1</strong>");
    // Bullet lines starting with - or *
    s = s.replace(/^[-*]\\s+(.+)$/gm, "\u2022 $1");
    // Newlines \u2192 <br>
    s = s.replace(/\\n/g, "<br>");
    return s;
  }

  function scrollBottom() {
    messagesEl.scrollTop = messagesEl.scrollHeight;
  }

  function addMessage(role, htmlContent) {
    const row = document.createElement("div");
    row.className = \`msg-row \${role}\`;
    const bubble = document.createElement("div");
    bubble.className = "bubble";
    bubble.innerHTML = htmlContent;
    row.appendChild(bubble);
    messagesEl.appendChild(row);
    scrollBottom();
    return row;
  }

  function addTypingIndicator() {
    const row = document.createElement("div");
    row.className = "msg-row assistant";
    row.innerHTML = \`
      <div class="bubble typing-dots">
        <span></span><span></span><span></span>
      </div>\`;
    messagesEl.appendChild(row);
    scrollBottom();
    return row;
  }

  function removeEl(el) {
    if (el && el.parentNode) el.parentNode.removeChild(el);
  }

  // ── DOM Operations ─────────────────────────────────────────────────────────
  function applyDomOperations(ops) {
    if (!Array.isArray(ops)) return;
    ops.forEach(function (op) {
      try {
        const el = document.querySelector(op.selector);
        if (!el) return;
        switch (op.op) {
          case "replace_content":
            el.innerHTML = op.html;
            break;
          case "replace_attr":
            el.setAttribute(op.attr, op.value);
            break;
          case "insert_after":
            el.insertAdjacentHTML("afterend", op.html);
            break;
          case "insert_before":
            el.insertAdjacentHTML("beforebegin", op.html);
            break;
          case "remove":
            el.remove();
            break;
          case "add_class":
            el.classList.add(...(Array.isArray(op.classes) ? op.classes : [op.classes]));
            break;
          case "remove_class":
            el.classList.remove(...(Array.isArray(op.classes) ? op.classes : [op.classes]));
            break;
          case "replace_style":
            Object.assign(el.style, op.styles);
            break;
        }
      } catch (e) {
        console.warn("[ChatWidget] DOM op failed:", op, e);
      }
    });
  }

  // ── Confirmation UI ────────────────────────────────────────────────────────
  function renderConfirmActions(msgRow, change) {
    // Remove any previously active confirmation buttons
    clearConfirmActions();

    pendingChange = change;

    const actions = document.createElement("div");
    actions.className = "confirm-actions";
    actions.id = "__pending-actions__";

    const prBtn = document.createElement("button");
    prBtn.className = "btn-confirm";
    prBtn.textContent = "\u2705 Create PR";

    const undoBtn = document.createElement("button");
    undoBtn.className = "btn-undo";
    undoBtn.textContent = "\u21a9\ufe0f Undo";

    actions.appendChild(prBtn);
    actions.appendChild(undoBtn);
    msgRow.appendChild(actions);
    pendingChangeMessageEl = actions;
    scrollBottom();

    prBtn.addEventListener("click", async function () {
      if (!pendingChange) return;
      const changeId = pendingChange.changeId;
      clearConfirmActions();
      await sendMessage("confirm", changeId);
    });

    undoBtn.addEventListener("click", function () {
      clearConfirmActions();
      addMessage("assistant", "Change reverted.");
      location.reload();
    });
  }

  function clearConfirmActions() {
    if (pendingChangeMessageEl) {
      removeEl(pendingChangeMessageEl);
      pendingChangeMessageEl = null;
    }
    pendingChange = null;
  }
})();
` }} />
      </body>
    </html>
  )
}
