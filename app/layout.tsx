import type { Metadata } from 'next'

import './globals.css'
import localFont from 'next/font/local'
const dosis = localFont({
  src: '../public/fonts/static/Dosis-SemiBold.ttf',
  display: 'swap',
  weight: '900',
})

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${dosis.className}`}>
        {children}
        <script data-liveedit-widget data-project="1a5ba3c7-3820-4bb7-b585-d57d42c9fba4" dangerouslySetInnerHTML={{ __html: `
(function () {
  const PROJECT_ID = "1a5ba3c7-3820-4bb7-b585-d57d42c9fba4";
  const API_ENDPOINT = "https://ai-editor-backend.vercel.app/api/chat";

  const conversation = [];

  const host = document.createElement('div');
  host.id = 'chat-widget-host';
  host.style.cssText = 'position:fixed;bottom:24px;right:24px;z-index:999999;display:flex;flex-direction:column;align-items:flex-end;';
  document.body.appendChild(host);

  const shadow = host.attachShadow({ mode: 'open' });

  const style = document.createElement('style');
  style.textContent = \`
    *{box-sizing:border-box;margin:0;padding:0;}
    .bubble{
      width:56px;height:56px;border-radius:50%;background:#1a1a1a;
      border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;
      box-shadow:0 4px 16px rgba(0,0,0,0.25);transition:transform 0.2s,background 0.2s;
      flex-shrink:0;
    }
    .bubble:hover{background:#333;transform:scale(1.08);}
    .bubble svg{width:26px;height:26px;fill:none;stroke:#fff;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}
    .panel{
      width:360px;height:520px;background:#fff;border-radius:16px;
      box-shadow:0 8px 40px rgba(0,0,0,0.18);display:flex;flex-direction:column;
      overflow:hidden;margin-bottom:12px;
      transform-origin:bottom right;
      transform:scale(0.85) translateY(20px);opacity:0;
      transition:transform 0.25s cubic-bezier(0.34,1.56,0.64,1),opacity 0.2s ease;
      pointer-events:none;
    }
    .panel.open{transform:scale(1) translateY(0);opacity:1;pointer-events:all;}
    .header{
      background:#1a1a1a;color:#fff;padding:16px 18px;
      display:flex;align-items:center;justify-content:space-between;flex-shrink:0;
    }
    .header-title{font-family:system-ui,sans-serif;font-size:15px;font-weight:600;letter-spacing:0.01em;}
    .close-btn{
      background:transparent;border:none;cursor:pointer;color:#fff;opacity:0.75;
      display:flex;align-items:center;justify-content:center;padding:2px;border-radius:4px;
      transition:opacity 0.15s;
    }
    .close-btn:hover{opacity:1;}
    .close-btn svg{width:20px;height:20px;stroke:#fff;stroke-width:2.2;stroke-linecap:round;}
    .messages{
      flex:1;overflow-y:auto;padding:16px 14px;display:flex;flex-direction:column;gap:10px;
      font-family:system-ui,sans-serif;font-size:14px;line-height:1.5;
      scroll-behavior:smooth;
    }
    .messages::-webkit-scrollbar{width:5px;}
    .messages::-webkit-scrollbar-track{background:transparent;}
    .messages::-webkit-scrollbar-thumb{background:#ddd;border-radius:99px;}
    .msg{max-width:80%;padding:10px 13px;border-radius:14px;word-break:break-word;}
    .msg.user{
      align-self:flex-end;background:#1a1a1a;color:#fff;
      border-bottom-right-radius:4px;
    }
    .msg.assistant{
      align-self:flex-start;background:#f0f0f0;color:#1a1a1a;
      border-bottom-left-radius:4px;
    }
    .msg.error{
      align-self:flex-start;background:#fdecea;color:#b71c1c;
      border-bottom-left-radius:4px;
    }
    .typing{align-self:flex-start;background:#f0f0f0;padding:10px 14px;border-radius:14px;border-bottom-left-radius:4px;display:flex;align-items:center;gap:5px;}
    .typing span{
      width:7px;height:7px;background:#999;border-radius:50%;display:inline-block;
      animation:bounce 1.2s infinite ease-in-out;
    }
    .typing span:nth-child(2){animation-delay:0.2s;}
    .typing span:nth-child(3){animation-delay:0.4s;}
    @keyframes bounce{
      0%,80%,100%{transform:translateY(0);}
      40%{transform:translateY(-6px);}
    }
    .input-row{
      display:flex;align-items:center;gap:8px;padding:12px 12px;
      border-top:1px solid #ebebeb;flex-shrink:0;background:#fff;
    }
    .input-field{
      flex:1;border:1.5px solid #e0e0e0;border-radius:10px;padding:9px 12px;
      font-family:system-ui,sans-serif;font-size:14px;outline:none;color:#1a1a1a;
      transition:border-color 0.15s;resize:none;background:#fafafa;
    }
    .input-field:focus{border-color:#1a1a1a;background:#fff;}
    .send-btn{
      width:40px;height:40px;border-radius:10px;background:#1a1a1a;border:none;
      cursor:pointer;display:flex;align-items:center;justify-content:center;
      flex-shrink:0;transition:background 0.15s,transform 0.1s;
    }
    .send-btn:hover{background:#333;}
    .send-btn:active{transform:scale(0.93);}
    .send-btn svg{width:18px;height:18px;stroke:#fff;stroke-width:2.2;stroke-linecap:round;stroke-linejoin:round;fill:none;}
    .send-btn:disabled{background:#ccc;cursor:not-allowed;}
  \`;
  shadow.appendChild(style);

  const panel = document.createElement('div');
  panel.className = 'panel';
  panel.innerHTML = \`
    <div class="header">
      <span class="header-title">Chat with us</span>
      <button class="close-btn" aria-label="Close chat">
        <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
    </div>
    <div class="messages"></div>
    <div class="input-row">
      <input class="input-field" type="text" placeholder="Type a message\u2026" autocomplete="off" aria-label="Message input"/>
      <button class="send-btn" aria-label="Send message">
        <svg viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
      </button>
    </div>
  \`;

  const bubble = document.createElement('button');
  bubble.className = 'bubble';
  bubble.setAttribute('aria-label', 'Open chat');
  bubble.innerHTML = \`
    <svg viewBox="0 0 24 24">
      <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
    </svg>
  \`;

  shadow.appendChild(panel);
  shadow.appendChild(bubble);

  const messagesEl = panel.querySelector('.messages');
  const inputField = panel.querySelector('.input-field');
  const sendBtn = panel.querySelector('.send-btn');
  const closeBtn = panel.querySelector('.close-btn');

  let isOpen = false;
  let isWaiting = false;

  function togglePanel() {
    isOpen = !isOpen;
    if (isOpen) {
      panel.classList.add('open');
      bubble.setAttribute('aria-label', 'Close chat');
      setTimeout(() => inputField.focus(), 250);
    } else {
      panel.classList.remove('open');
      bubble.setAttribute('aria-label', 'Open chat');
    }
  }

  bubble.addEventListener('click', togglePanel);
  closeBtn.addEventListener('click', togglePanel);

  function scrollToBottom() {
    messagesEl.scrollTop = messagesEl.scrollHeight;
  }

  function appendMessage(role, text) {
    const el = document.createElement('div');
    el.className = 'msg ' + role;
    el.textContent = text;
    messagesEl.appendChild(el);
    scrollToBottom();
    return el;
  }

  function showTyping() {
    const el = document.createElement('div');
    el.className = 'typing';
    el.innerHTML = '<span></span><span></span><span></span>';
    messagesEl.appendChild(el);
    scrollToBottom();
    return el;
  }

  function setWaiting(val) {
    isWaiting = val;
    sendBtn.disabled = val;
    inputField.disabled = val;
  }

  async function sendMessage() {
    const text = inputField.value.trim();
    if (!text || isWaiting) return;

    inputField.value = '';
    conversation.push({ role: 'user', content: text });
    appendMessage('user', text);
    setWaiting(true);

    const typingEl = showTyping();

    try {
      const response = await fetch(API_ENDPOINT, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ projectId: PROJECT_ID, message: text })
      });

      typingEl.remove();

      if (!response.ok) {
        throw new Error('Network response was not ok');
      }

      const data = await response.json();
      const reply = data.reply || '';
      conversation.push({ role: 'assistant', content: reply });
      appendMessage('assistant', reply);
    } catch (e) {
      typingEl.remove();
      appendMessage('error', 'Sorry, something went wrong. Please try again.');
    } finally {
      setWaiting(false);
      inputField.focus();
    }
  }

  sendBtn.addEventListener('click', sendMessage);
  inputField.addEventListener('keydown', function (e) {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      sendMessage();
    }
  });
})();
` }} />
      </body>
    </html>
  )
}
