import type { Metadata } from 'next'
import Script from 'next/script'
import './globals.css'
import localFont from 'next/font/local'

const dosis = localFont({
  src: '../public/fonts/static/Dosis-SemiBold.ttf',
  display: 'swap',
  weight: '900',
})

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
}

const WIDGET_URL = 'https://ai-editor-backend-vsqh.onrender.com/api/snippet/widget/48cb8dd7-0c81-4727-8256-13acd5e976e7'

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={dosis.className}>
        {children}
        
      
        
      
        <script data-liveedit-widget data-project="48cb8dd7-0c81-4727-8256-13acd5e976e7">
(function() {
  const PROJECT_ID = "48cb8dd7-0c81-4727-8256-13acd5e976e7";
  const API_ENDPOINT = "https://ai-editor-backend-vsqh.onrender.com/api/chat";

  const host = document.createElement('div');
  host.id = 'ai-chat-widget-host';
  host.style.cssText = 'position:fixed;bottom:24px;right:24px;z-index:999999;display:flex;flex-direction:column;align-items:flex-end;';
  document.body.appendChild(host);

  const shadow = host.attachShadow({ mode: 'open' });

  const style = document.createElement('style');
  style.textContent = `
    *{box-sizing:border-box;margin:0;padding:0;}
    .bubble{width:56px;height:56px;border-radius:50%;background:#1a1a1a;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:0 4px 16px rgba(0,0,0,0.25);transition:transform 0.2s,box-shadow 0.2s;flex-shrink:0;}
    .bubble:hover{transform:scale(1.08);box-shadow:0 6px 22px rgba(0,0,0,0.32);}
    .bubble svg{width:26px;height:26px;fill:white;}
    .panel{width:360px;height:520px;background:#fff;border-radius:16px;box-shadow:0 8px 40px rgba(0,0,0,0.18);display:flex;flex-direction:column;overflow:hidden;margin-bottom:12px;transform-origin:bottom right;transition:transform 0.28s cubic-bezier(0.34,1.56,0.64,1),opacity 0.22s ease;}
    .panel.hidden{transform:scale(0.85) translateY(30px);opacity:0;pointer-events:none;}
    .panel.visible{transform:scale(1) translateY(0);opacity:1;pointer-events:all;}
    .header{background:#1a1a1a;color:#fff;padding:0 16px;height:54px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0;}
    .header-title{font-family:system-ui,-apple-system,sans-serif;font-size:15px;font-weight:600;letter-spacing:0.01em;}
    .close-btn{background:none;border:none;cursor:pointer;color:#fff;display:flex;align-items:center;justify-content:center;padding:4px;border-radius:6px;transition:background 0.15s;}
    .close-btn:hover{background:rgba(255,255,255,0.15);}
    .close-btn svg{width:18px;height:18px;stroke:white;fill:none;}
    .messages{flex:1;overflow-y:auto;padding:16px;display:flex;flex-direction:column;gap:10px;font-family:system-ui,-apple-system,sans-serif;font-size:14px;line-height:1.5;}
    .messages::-webkit-scrollbar{width:5px;}
    .messages::-webkit-scrollbar-track{background:transparent;}
    .messages::-webkit-scrollbar-thumb{background:#ddd;border-radius:10px;}
    .msg{max-width:82%;padding:10px 13px;border-radius:14px;word-wrap:break-word;animation:fadeUp 0.2s ease;}
    @keyframes fadeUp{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:translateY(0);}}
    .msg.user{align-self:flex-end;background:#1a1a1a;color:#fff;border-bottom-right-radius:4px;}
    .msg.assistant{align-self:flex-start;background:#f0f0f0;color:#1a1a1a;border-bottom-left-radius:4px;}
    .msg.error{align-self:flex-start;background:#fff0f0;color:#c0392b;border:1px solid #f5c6c6;border-bottom-left-radius:4px;}
    .typing{align-self:flex-start;background:#f0f0f0;border-radius:14px;border-bottom-left-radius:4px;padding:12px 16px;display:flex;align-items:center;gap:5px;}
    .typing span{width:7px;height:7px;border-radius:50%;background:#999;display:inline-block;animation:bounce 1.2s infinite ease-in-out;}
    .typing span:nth-child(2){animation-delay:0.2s;}
    .typing span:nth-child(3){animation-delay:0.4s;}
    @keyframes bounce{0%,80%,100%{transform:translateY(0);}40%{transform:translateY(-7px);}}
    .input-row{display:flex;align-items:center;gap:8px;padding:10px 12px;border-top:1px solid #ebebeb;flex-shrink:0;background:#fff;}
    .input-field{flex:1;border:1px solid #ddd;border-radius:10px;padding:9px 12px;font-size:14px;font-family:system-ui,-apple-system,sans-serif;outline:none;resize:none;line-height:1.4;max-height:100px;overflow-y:auto;transition:border 0.15s;}
    .input-field:focus{border-color:#1a1a1a;}
    .send-btn{width:38px;height:38px;border-radius:10px;background:#1a1a1a;border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;transition:background 0.15s,transform 0.1s;}
    .send-btn:hover{background:#333;}
    .send-btn:active{transform:scale(0.93);}
    .send-btn svg{width:17px;height:17px;fill:white;}
    .send-btn:disabled{background:#ccc;cursor:not-allowed;}
    .confirm-row{display:flex;gap:8px;margin-top:6px;flex-wrap:wrap;}
    .confirm-btn{padding:7px 13px;border-radius:8px;border:none;cursor:pointer;font-size:13px;font-family:system-ui,-apple-system,sans-serif;font-weight:500;transition:opacity 0.15s;}
    .confirm-btn.pr{background:#1a1a1a;color:#fff;}
    .confirm-btn.undo{background:#f0f0f0;color:#1a1a1a;}
    .confirm-btn:hover{opacity:0.82;}
  `;
  shadow.appendChild(style);

  const panel = document.createElement('div');
  panel.className = 'panel hidden';
  panel.innerHTML = `
    <div class="header">
      <span class="header-title">Chat with us</span>
      <button class="close-btn" aria-label="Close chat">
        <svg viewBox="0 0 24 24" stroke-width="2.5" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
    </div>
    <div class="messages" id="messages"></div>
    <div class="input-row">
      <textarea class="input-field" id="inputField" placeholder="Type a message…" rows="1"></textarea>
      <button class="send-btn" id="sendBtn" aria-label="Send">
        <svg viewBox="0 0 24 24"><path d="M2 21l21-9L2 3v7l15 2-15 2v7z"/></svg>
      </button>
    </div>
  `;
  shadow.appendChild(panel);

  const bubble = document.createElement('button');
  bubble.className = 'bubble';
  bubble.setAttribute('aria-label', 'Open chat');
  bubble.innerHTML = `<svg viewBox="0 0 24 24"><path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H5.17L4 17.17V4h16v12z"/></svg>`;
  shadow.appendChild(bubble);

  const messagesEl = shadow.getElementById('messages');
  const inputField = shadow.getElementById('inputField');
  const sendBtn = shadow.getElementById('sendBtn');
  const closeBtn = panel.querySelector('.close-btn');

  let isOpen = false;
  let isWaiting = false;
  let conversation = [];
  let currentPendingChange = null;

  function togglePanel(open) {
    isOpen = open !== undefined ? open : !isOpen;
    if (isOpen) {
      panel.classList.remove('hidden');
      panel.classList.add('visible');
      bubble.setAttribute('aria-label', 'Close chat');
      inputField.focus();
    } else {
      panel.classList.remove('visible');
      panel.classList.add('hidden');
      bubble.setAttribute('aria-label', 'Open chat');
    }
  }

  bubble.addEventListener('click', () => togglePanel());
  closeBtn.addEventListener('click', () => togglePanel(false));

  function scrollToBottom() {
    messagesEl.scrollTop = messagesEl.scrollHeight;
  }

  function renderMarkdown(text) {
    return text
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
      .replace(/\n/g, '<br>');
  }

  function appendMessage(role, text, extra) {
    const div = document.createElement('div');
    div.className = 'msg ' + (role === 'user' ? 'user' : role === 'error' ? 'error' : 'assistant');
    div.innerHTML = renderMarkdown(text);
    if (extra) div.appendChild(extra);
    messagesEl.appendChild(div);
    scrollToBottom();
    return div;
  }

  function showTyping() {
    const dots = document.createElement('div');
    dots.className = 'typing';
    dots.innerHTML = '<span></span><span></span><span></span>';
    messagesEl.appendChild(dots);
    scrollToBottom();
    return dots;
  }

  function applyDomOperations(ops) {
    if (!ops || !Array.isArray(ops)) return;
    ops.forEach(op => {
      try {
        const el = document.querySelector(op.selector);
        if (!el) return;
        if (op.op === 'replace_content') el.innerHTML = op.html;
        else if (op.op === 'replace_attr') el.setAttribute(op.attr, op.value);
        else if (op.op === 'replace_style') Object.assign(el.style, op.styles);
        else if (op.op === 'add_class') el.classList.add(...op.classes);
        else if (op.op === 'remove_class') el.classList.remove(...op.classes);
        else if (op.op === 'insert_after') el.insertAdjacentHTML('afterend', op.html);
        else if (op.op === 'remove') el.remove();
      } catch(e) {}
    });
  }

  function buildConfirmRow(pendingChange) {
    const row = document.createElement('div');
    row.className = 'confirm-row';

    const prBtn = document.createElement('button');
    prBtn.className = 'confirm-btn pr';
    prBtn.textContent = '✅ Create PR';

    const undoBtn = document.createElement('button');
    undoBtn.className = 'confirm-btn undo';
    undoBtn.textContent = '↩️ Undo';

    prBtn.addEventListener('click', async () => {
      prBtn.disabled = true;
      undoBtn.disabled = true;
      const typing = showTyping();
      try {
        const res = await fetch(API_ENDPOINT, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ projectId: PROJECT_ID, message: 'confirm', confirmChangeId: pendingChange.id })
        });
        const data = await res.json();
        typing.remove();
        if (data.reply) {
          conversation.push({ role: 'assistant', content: data.reply });
          appendMessage('assistant', data.reply);
        }
        if (data.pendingChange) {
          currentPendingChange = data.pendingChange;
        }
      } catch(e) {
        typing.remove();
        appendMessage('error', 'Sorry, something went wrong. Please try again.');
      }
    });

    undoBtn.addEventListener('click', () => {
      location.reload();
    });

    row.appendChild(prBtn);
    row.appendChild(undoBtn);
    return row;
  }

  async function sendMessage() {
    const text = inputField.value.trim();
    if (!text || isWaiting) return;

    inputField.value = '';
    inputField.style.height = 'auto';
    isWaiting = true;
    sendBtn.disabled = true;

    conversation.push({ role: 'user', content: text });
    appendMessage('user', text);

    const typing = showTyping();

    try {
      var pageHtml = document.documentElement.outerHTML.slice(0, 200000);
      const res = await fetch(API_ENDPOINT, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ projectId: PROJECT_ID, message: text, pageHtml })
      });
      const data = await res.json();
      typing.remove();

      if (data.domOperations) {
        applyDomOperations(data.domOperations);
      }

      let extra = null;
      if (data.pendingChange) {
        currentPendingChange = data.pendingChange;
        extra = buildConfirmRow(data.pendingChange);
      }

      const reply = data.reply || '';
      if (reply) {
        conversation.push({ role: 'assistant', content: reply });
        appendMessage('assistant', reply, extra);
      } else if (extra) {
        const div = document.createElement('div');
        div.className = 'msg assistant';
        div.appendChild(extra);
        messagesEl.appendChild(div);
        scrollToBottom();
      }

    } catch(e) {
      typing.remove();
      appendMessage('error', 'Sorry, something went wrong. Please try again.');
    }

    isWaiting = false;
    sendBtn.disabled = false;
    inputField.focus();
  }

  sendBtn.addEventListener('click', sendMessage);

  inputField.addEventListener('keydown', e => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      sendMessage();
    }
  });

  inputField.addEventListener('input', () => {
    inputField.style.height = 'auto';
    inputField.style.height = Math.min(inputField.scrollHeight, 100) + 'px';
  });

})();
</script>
      </body>
    </html>
  )
}
